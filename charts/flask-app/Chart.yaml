apiVersion: v2
name: flask-app
version: 0.1.0
dependencies:
  # PostgreSQL от Bitnami через OCI (работает!)
  - name: postgresql
    version: "12.6.0"
    repository: oci://registry-1.docker.io/bitnamicharts
    
  # Redis от groundhog2k (простая и надежная альтернатива)
  - name: redis
    version: "2.0.5"
    repository: https://groundhog2k.github.io/helm-charts/
