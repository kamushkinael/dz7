apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.name }}-ingress  # <--- УНИКАЛЬНОЕ ИМЯ
spec:
  rules:
  - host: flask-app.local
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: {{ .Values.name }}
            port:
              number: {{ .Values.service.port }}
