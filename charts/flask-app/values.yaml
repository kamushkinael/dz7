# Flask приложение
name: flask-app
replicaCount: 2

image:
  repository: kamushkinael/flask-app
  tag: latest
  pullPolicy: Always

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "200m"

service:
  type: NodePort
  port: 80
